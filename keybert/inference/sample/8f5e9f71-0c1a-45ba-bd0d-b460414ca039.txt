基于正则化奇异值分解(RSVD)的推荐算法在预测准确性上具有明显优势，但存在计算量大的缺点，一定程度上限制了其在实际工程中的应用。针对这个问题，该文提出一种基于标签迁移学习的改进RSVD推荐算法，在相对稠密的辅助数据集合上根据标签信息来提取用户/项目特征，然后将用户/项目特征应用到RSVD算法中，在目标数据集合上进行评分预测。在MovieLens数据集合上的实验结果表明，该算法能够缓解数据稀疏性问题，显著降低预测误差(约0.01RMSE)，同时减少约50%的训练时间。